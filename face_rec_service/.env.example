# Development face-service authentication.
# Must exactly match backend FACE_API_KEY when backend proxies requests here.
FACE_API_KEY=facekey

# Optional; production enables authentication automatically.
ENVIRONMENT=development
