name: Refresh web and mobile lockfiles

on:
  push:
    branches: [tmp/lock-refresh-v4]
    paths:
      - 'frontend/package.json'
      - 'frontend/package-lock.json'
      - 'mobile/package.json'
      - 'mobile/package-lock.json'
      - '.github/workflows/lock-refresh.yml'

permissions:
  contents: write

jobs:
  refresh:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false
      - working-directory: frontend
        run: npm install --package-lock-only --ignore-scripts --legacy-peer-deps
      - working-directory: mobile
        run: npm install --package-lock-only --ignore-scripts --legacy-peer-deps
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add frontend/package-lock.json mobile/package-lock.json
          git commit -m 'chore: synchronize web and mobile lockfiles [skip ci]'
          git push origin HEAD:tmp/lock-refresh-v4
